// ignore_for_file: file_names
import 'package:flutter/material.dart';

class BSLogo extends StatelessWidget {
  const BSLogo({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('BS', style: Theme.of(context).textTheme.headlineLarge);
  }
}
